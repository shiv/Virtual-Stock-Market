{
  "name": "ttp-stock-heroku-deploy",
  "version": "0.1.0",
  "scripts": {
    "start": "node ./server/app.js",
    "heroku-postbuild": "cd client && yarn && yarn build"
  },
  "dependencies": {
    "yarn": "^1.16.0"
  }
}
